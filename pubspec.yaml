name: proyecto_qpon
description: "Qpon - Flutter app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^4.18.2
  firebase_core: ^2.24.2
  google_sign_in: ^6.1.5
  cloud_firestore: ^4.13.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
